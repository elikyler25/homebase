---
description: Start homebase — curate plugins, discover projects, pick which to work on, then plan/dispatch/verify work across them
---

Invoke the `homebase-setup` skill and follow it exactly.

Do **Step 0 (curate the workspace)** first — ensure ponytail + obsidian-second-brain are
available and trim the loaded plugins to the lean core so tokens stay focused. Then discover
my projects, present a numbered list (plus any new candidates), and ask which I want to work
on this session. Do not dispatch any work until I've chosen.

# imagegen ML stack — installed on YOUR machine, not part of the homebase plugin.
#
# torch is intentionally unpinned here: the right build depends on your platform
# (CUDA / ROCm / Apple MPS / CPU). Install torch FIRST from the official selector
# so you get the matching accelerator build, THEN install the rest:
#
#   https://pytorch.org/get-started/locally/
#   # e.g. NVIDIA/CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   # e.g. Apple Silicon or CPU-only:
#   pip install torch
#
# Then:  pip install -r imagegen/requirements.txt

torch>=2.1
diffusers>=0.30
transformers>=4.40
accelerate>=0.30
safetensors>=0.4
Pillow>=10.0
# Needed by FLUX / SD3 text encoders (T5); harmless for SDXL.
sentencepiece>=0.2
protobuf>=4.25
